@model MyPortfolioWebApp.Models.ViewModels.ProjectsListViewModel

<section class="ProjectList" style="background: white;">
    <div class="container">
        <div class="row">
            <div class="col text-center">
                <h2>Moje projekty:</h2>
            </div>
        </div>
    


        @foreach (var t in Model.List)
        {
                @Html.Partial("_ProjectCardPartialView", t)
        }

    </div>
</section>


